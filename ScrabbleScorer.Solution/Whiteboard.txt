["A", "E", I, O, U, L, N, R, S, T]      += 1
[D, G ]                             += 2
B, C, M, P                         3
F, H, V, W, Y                      4
K                                  5
J, X                               8
Q, Z                               10

list<char> listPoint1 = new List<char> {'A', 'E', 'I', 'O','L','N','R','S','T'}
list<char> listPoint2 = new List<char> {'D', 'G',}
list<char> listPoint3 = new List<char> {'B','C','M','P'}
list<char> listPoint4 = new List<char> {'F','H','V','W','Y'}
list<char> listPoint5 = new List<char> {'J','X'};
list<char> listPoint6 = new List<char> {'Q','Z'}

WHEN BACK FROM LUNCH: create a function that can interact with the list and single letters/characters/etc.



characterArray[] WordArray = FinalInput.ToCharArray();

int score = 0;
foreach (char i in WordArray)
 if (listPoint1.Contains(i))
    {
        score += 1;
    }
    else if (listPoint2.Contains(i))
    {
        score +=2
    }
    else if (listPoint3.Co)
}

Make a console application that takes a word and returns the Scrabble score for that word. 
toUpperCase();
7 separate Char[] Array = set individual character value

take in a string and break it up into characters array

take those characters from user submitted word and assign value

for (int i = 0, i < array.lenght(), i++) 
{
    userinput.array[i]
    if (userinput.array[i] == array1[i] <-this is wrong most likely) {
        score++;
    }
}